#!/bin/bash

ConfigureOpts+=(
    -Dglycin-loaders=true # Build loaders
    -Dloaders="glycin-heif,glycin-image-rs,glycin-jxl,glycin-svg"
        # List of loaders to build. Only has an effect if "glycin-loaders" is enabled
        # choices:
        # ['glycin-heif', 'glycin-image-rs', 'glycin-jxl', 'glycin-raw', 'glycin-svg', 'glycin-test', ],
        # value:
        # ['glycin-heif', 'glycin-image-rs', 'glycin-jxl', 'glycin-svg'],
)
